def is_palindrome(l):
    if len(l) % 2 == 0:
        for i in range(int(len(l) / 2)):
            if l[i] != l[-1 - i]: 
                return False
    for i in range(int(len(l) / 2 + 0.5)):
            if l[i] != l[-1 - i]:  
                return False 
    return True
