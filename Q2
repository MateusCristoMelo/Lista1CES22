def draw_poly(t, n, sz):
    wn = turtle.Screen()
    wn.bgcolor("lightgreen")
    t.pencolor("red")
    for i in range(n):
        t.forward(sz)
        t.left(360 / n)
    wn.mainloop()
    return
   
